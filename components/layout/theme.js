import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#005',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});

export default theme