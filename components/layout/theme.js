import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#005',
      light : '#26266e'
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});

export default theme